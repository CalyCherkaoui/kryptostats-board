/* eslint-disable arrow-body-style */
import React from 'react';

const AddCoinToTrack = () => {
  return (
    <div>
      <button type="button">Add coin to track</button>
    </div>
  );
};

export default AddCoinToTrack;
